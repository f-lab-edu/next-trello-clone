/* / 루트 page 컴포넌트 */
// import
import DragDrop from "@/components/DragDrop";
import Form from "@/components/Form";

// component
const Home = () => {
  return (
    <div>
      {/* 외부 컴포넌트 적용 */}
      <DragDrop />
    </div>
  );
};

// export
export default Home;
